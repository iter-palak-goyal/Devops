pipeline {
    agent any

    stages {

        stage('Build') {
            steps {
                dir('Maven1') {
                    sh 'mvn clean package'
                }
            }
        }

    }
}